# 简化配置文件用于Web服务
# YOLO11模型配置
yolo11_model_path: assets/best_openvino_model/best.xml
device: CPU
min_confidence: 0.7
threshold: 150

# ROI配置 (不使用ROI)
use_roi: false
roi:
  x: 0
  y: 0
  width: -1
  height: -1

# 分类器配置 (如果需要)
classify_model: assets/tiny_resnet.onnx
